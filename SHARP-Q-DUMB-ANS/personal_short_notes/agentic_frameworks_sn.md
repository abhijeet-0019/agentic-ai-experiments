Design Angle:
- define tools | input and output matters
- define tracing function | simple print that outputs the current state OR data/messages that is going to get feed next (in the current case it is always the state only) 
- the initial schema do vary as per the different LLM API -- thats why fameworks

============
QUESTIONS:
- does different strutre of loop, and function call handling, traching vary as per the LLM API from differnt vendors, does that also contibutes in to going towards the framework, so that this llm vendor lock in does not happen

- 
